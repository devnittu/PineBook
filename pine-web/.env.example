# Frontend — Environment Variables
# Copy to .env and configure

# Use mock data (true = mock, false = real backend)
VITE_USE_MOCK=true

# API base URL (used when VITE_USE_MOCK=false)
VITE_API_BASE_URL=http://localhost:8080
